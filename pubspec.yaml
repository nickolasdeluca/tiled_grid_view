name: tiled_grid_view
description: A flexible tiled grid view layout system for Flutter with automatic row distribution and layout warnings.
version: 1.0.2
homepage: https://github.com/nickolasdeluca/tiled_grid_view
repository: https://github.com/nickolasdeluca/tiled_grid_view
issue_tracker: https://github.com/nickolasdeluca/tiled_grid_view/issues

topics:
  - grid
  - layout
  - tiles
  - ui
  - widgets

environment:
  sdk: ^3.10.7
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
